Generate the single-file HTML application adhering to all System Instructions. The application should fetch and display the data from a JSON file named `taittiriya-upanishad-commentary.json` located in the same directory as the HTML file.